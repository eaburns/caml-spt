* Drawing legends off of the plot.

* Change text size based on plot size

* Scatter plot matrix

* Increase/decrease the number of error bars with the size of the plot?

* Change minor ticks to be at intervals other than the center of major
  ticks.

------------------------------------------------------------
Datasets
------------------------------------------------------------

* Line errorbar plots should throw out a *nasty* warning if the data
  is not sorted by x-value (since the errorbars may not center on the
  mean line).

* Find a better way to denote groups in numeric by nominal plots.

* "real" kernel density estimation datasets
   - make an object for it that will find dimensions
   - find the bandwidth automatically (optional)

* "boxplots with density estimations"

* Normal quantile plots

* CDF's need error bars

* Heatmaps - Nominal by Nominal

* Montages: more complex than simple ps-montage style.
  - Make axis drawing optional.
  - Make room for non-drawn axes, but then don't draw them.

* Labels may be backwards in layered barcharts

------------------------------------------------------------
Documentation
------------------------------------------------------------

* Write a real README file

* Document all functions
  - Generate OCaml docs

* Manual for spt-it-out

* How to use within AI group

------------------------------------------------------------
Usability
------------------------------------------------------------

* GUI dialog for changing plot sizes could be better

* Function wrappers for creating plots (instead of using 'new')

* Generate .mli files where possible / appropriate

* Bindings
  - C
  - Python
  - Java?

------------------------------------------------------------
spt-it-out
------------------------------------------------------------

* Grouping num by nom datasets in spt-it-out

* label datasets

* scatter with errbar datasets

* Including other spt-it-out files

* Barchat datasets of various kinds

* The two kinds of heatmap datasets

* Simple arithmetic... mapping log10 over things, etc.

------------------------------------------------------------
Plot types
------------------------------------------------------------

* Nominal by nominal plots

* Numeric by Ordinal (or Ranked) plots
  Like numeric by nominal except that x-axis is a set of ranked values
  and the datasets are drawn across both the x and y axes.
  - Waterfall charts
  - Run charts
